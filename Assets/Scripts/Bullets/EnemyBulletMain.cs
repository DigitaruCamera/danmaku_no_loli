using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyBulletMain : MonoBehaviour {

    /* spawner avec la bullet avec le rotation corriger et a la hauteur du joueur
       partir en direction du joueur
       suivre le joueur
       pouvoir faire spawn d'autre bullet (suivant plusieur condition)
       suivre une fonction type f(x)
       tu vois vois les boss fait des zone, puis y a des meteorite qui tombe dedans et ba pareil
       mode railgun lazer


      
        pourvoir controller la vitesse/taille/etc suivant condition ou curve
        https://docs.unity3d.com/ScriptReference/EditorGUI.CurveField.html
       */

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
